/** Official Coach Connect brand mark (CG + bicep, neon on black). */
export const BRAND_LOGO_SOURCE = require('./Logo.PNG');

/** Native asset aspect ratio — width / height. */
export const BRAND_LOGO_ASPECT = 1360 / 1156;
