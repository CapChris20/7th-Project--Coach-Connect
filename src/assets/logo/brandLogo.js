/** Official Coach Connect brand mark (interlocking CC infinity gradient). */
export const BRAND_LOGO_SOURCE = require('./Logo.PNG');

/** Native asset aspect ratio — width / height. */
export const BRAND_LOGO_ASPECT = 1;
